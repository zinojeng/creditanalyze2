{
  "run": "streamlit run main.py --server.port $PORT --server.address 0.0.0.0",
  "build": {
    "buildCommand": "apt-get update && apt-get install -y tesseract-ocr libtesseract-dev poppler-utils libgl1-mesa-glx libsm6 libxext6 libxrender-dev libglib2.0-0 && pip install -r requirements.txt",
    "outputDirectory": "."
  },
  "env": {
    "STREAMLIT_SERVER_PORT": "$PORT",
    "STREAMLIT_SERVER_ENABLE_CORS": "false",
    "STREAMLIT_SERVER_HEADLESS": "true",
    "PYTHONUNBUFFERED": "true"
  }
} 